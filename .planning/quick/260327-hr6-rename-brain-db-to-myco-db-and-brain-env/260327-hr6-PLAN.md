---
phase: quick
plan: 260327-hr6
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/db.ts
  - packages/core/src/config.ts
  - packages/mcp-server/src/consolidator.ts
  - .claude/hooks/gsd-brain-episode.js
  - .claude/hooks/gsd-myco-episode.js
  - .claude/settings.json
  - packages/core/tests/db.test.ts
  - packages/mcp-server/tests/gsd-brain-episode.test.ts
  - packages/mcp-server/tests/gsd-myco-episode.test.ts
  - README.md
  - GETTING-STARTED.md
  - QA-CHECKLIST.md
  - CHANGELOG.md
  - .env.example
autonomous: true
requirements: []
must_haves:
  truths:
    - "No references to brain.db remain in source code, tests, or docs (excluding .planning/ history)"
    - "No references to BRAIN_DB_PATH or BRAIN_CONSOLIDATION_MODEL remain in source code"
    - "No references to mcpServers.brain remain in hook files"
    - "All tests pass after renaming"
  artifacts:
    - path: ".claude/hooks/gsd-myco-episode.js"
      provides: "Renamed hook file (was gsd-brain-episode.js)"
    - path: "packages/mcp-server/tests/gsd-myco-episode.test.ts"
      provides: "Renamed test file (was gsd-brain-episode.test.ts)"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/gsd-myco-episode.js"
      via: "hook path reference"
      pattern: "gsd-myco-episode"
---

<objective>
Rename all remaining "brain" references to "myco" across the entire codebase for open source branding consistency.

Purpose: Complete the brain-to-myco rename started in prior commits. Remove all BRAIN_* env var fallbacks and brain.db references. Clean break, no backwards compatibility.
Output: Fully renamed codebase with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/core/src/db.ts
@packages/core/src/config.ts
@packages/mcp-server/src/consolidator.ts
@.claude/hooks/gsd-brain-episode.js
@.claude/settings.json
@packages/core/tests/db.test.ts
@packages/mcp-server/tests/gsd-brain-episode.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename brain references in source code and hooks</name>
  <files>
    packages/core/src/db.ts
    packages/core/src/config.ts
    packages/mcp-server/src/consolidator.ts
    .claude/hooks/gsd-brain-episode.js -> .claude/hooks/gsd-myco-episode.js
    .claude/settings.json
    ~/.claude/hooks/myco-session-start.js
    ~/.claude/hooks/myco-auto-remember.js
  </files>
  <action>
    **Source code changes:**
    1. `packages/core/src/db.ts:10` — change `'brain.db'` to `'myco.db'`
    2. `packages/core/src/db.ts:14` — remove `process.env.BRAIN_DB_PATH` fallback entirely (MYCO_DB_PATH is already primary)
    3. `packages/core/src/config.ts:25` — remove `process.env.BRAIN_DB_PATH` fallback
    4. `packages/core/src/config.ts:28` — rename `BRAIN_CONSOLIDATION_MODEL` to `MYCO_CONSOLIDATION_MODEL`
    5. `packages/mcp-server/src/consolidator.ts:19` — rename `BRAIN_CONSOLIDATION_MODEL` to `MYCO_CONSOLIDATION_MODEL`

    **Hook file rename:**
    6. Copy `.claude/hooks/gsd-brain-episode.js` to `.claude/hooks/gsd-myco-episode.js`
    7. In the new file: replace all `brain.db` with `myco.db`, remove any `BRAIN_DB_PATH` fallback, update comments referencing "brain"
    8. Delete `.claude/hooks/gsd-brain-episode.js`
    9. Update `.claude/settings.json` hook path from `gsd-brain-episode.js` to `gsd-myco-episode.js`

    **External hook files (outside repo, at ~/.claude/hooks/):**
    10. `~/.claude/hooks/myco-session-start.js` — change `brain.db` to `myco.db`, remove `BRAIN_DB_PATH` fallback, rename `hasBrainServer` to `hasMycoServer`, update comments
    11. `~/.claude/hooks/myco-auto-remember.js` — remove any `mcpServers.brain` checks (only check `mcpServers.myco`)
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/myco && grep -r "brain\.db\|BRAIN_DB_PATH\|BRAIN_CONSOLIDATION_MODEL\|hasBrainServer\|gsd-brain-episode" --include="*.ts" --include="*.js" --include="*.json" packages/ .claude/ | grep -v node_modules | grep -v ".planning/" ; echo "Exit: $?"</automated>
  </verify>
  <done>Zero grep matches for brain.db, BRAIN_DB_PATH, BRAIN_CONSOLIDATION_MODEL, hasBrainServer, or gsd-brain-episode in source, hooks, and settings. External hooks at ~/.claude/hooks/ also updated.</done>
</task>

<task type="auto">
  <name>Task 2: Rename brain references in tests and docs</name>
  <files>
    packages/core/tests/db.test.ts
    packages/mcp-server/tests/gsd-brain-episode.test.ts -> packages/mcp-server/tests/gsd-myco-episode.test.ts
    README.md
    GETTING-STARTED.md
    QA-CHECKLIST.md
    CHANGELOG.md
    .env.example
  </files>
  <action>
    **Test file changes:**
    1. `packages/core/tests/db.test.ts` — update all `brain.db` assertions to `myco.db`, remove any BRAIN_DB_PATH fallback test cases, update BRAIN_CONSOLIDATION_MODEL to MYCO_CONSOLIDATION_MODEL in env var tests
    2. Rename `packages/mcp-server/tests/gsd-brain-episode.test.ts` to `gsd-myco-episode.test.ts` (git mv)
    3. In the renamed test file: replace all `brain.db` with `myco.db`, remove BRAIN_DB_PATH references, update temp dir names from "brain" to "myco", update import path if it references the old hook filename, update comments

    **Documentation changes:**
    4. `README.md` — update architecture diagram `brain.db` to `myco.db`, env var table (remove BRAIN_DB_PATH fallback mention, rename BRAIN_CONSOLIDATION_MODEL to MYCO_CONSOLIDATION_MODEL)
    5. `GETTING-STARTED.md` — same updates: architecture diagram, file locations table, env vars table
    6. `QA-CHECKLIST.md` — update db paths and BRAIN_CONSOLIDATION_MODEL reference
    7. `CHANGELOG.md:14` — update BRAIN_CONSOLIDATION_MODEL mention
    8. `.env.example` — update brain.db comment, remove BRAIN_DB_PATH, rename BRAIN_CONSOLIDATION_MODEL to MYCO_CONSOLIDATION_MODEL
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/myco && grep -r "brain\.db\|BRAIN_DB_PATH\|BRAIN_CONSOLIDATION_MODEL\|gsd-brain-episode" --include="*.ts" --include="*.js" --include="*.md" --include="*.example" . | grep -v node_modules | grep -v ".planning/" ; echo "Exit: $?" && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>Zero grep matches across entire codebase (excluding .planning/ history). All tests pass — db.test.ts validates myco.db path, renamed hook test file passes.</done>
</task>

</tasks>

<verification>
Final comprehensive check:

1. `grep -rn "brain" --include="*.ts" --include="*.js" --include="*.json" --include="*.md" --include="*.example" . | grep -v node_modules | grep -v ".planning/" | grep -v "CHANGELOG.*prior\|CHANGELOG.*history"` — should return zero matches (or only historical changelog prose that doesn't reference env vars or file paths)
2. `npm test` — all test suites pass
3. `ls .claude/hooks/gsd-myco-episode.js` — renamed hook exists
4. `ls packages/mcp-server/tests/gsd-myco-episode.test.ts` — renamed test exists
5. `! ls .claude/hooks/gsd-brain-episode.js 2>/dev/null` — old hook file deleted
6. `! ls packages/mcp-server/tests/gsd-brain-episode.test.ts 2>/dev/null` — old test file deleted
</verification>

<success_criteria>
- All "brain" references removed from source, hooks, tests, and docs (excluding .planning/ historical records)
- BRAIN_DB_PATH fallback removed everywhere (clean break)
- BRAIN_CONSOLIDATION_MODEL renamed to MYCO_CONSOLIDATION_MODEL everywhere
- Hook file renamed gsd-brain-episode.js to gsd-myco-episode.js with settings.json updated
- Test file renamed gsd-brain-episode.test.ts to gsd-myco-episode.test.ts
- External hooks (~/.claude/hooks/) updated
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260327-hr6-rename-brain-db-to-myco-db-and-brain-env/260327-hr6-SUMMARY.md`
</output>

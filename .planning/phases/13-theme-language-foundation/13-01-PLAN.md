---
phase: 13-theme-language-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/routes/__root.tsx
  - packages/dashboard/src/routes/approvals.tsx
  - packages/dashboard/src/components/activity-feed.tsx
  - packages/dashboard/src/components/quick-approve.tsx
  - packages/dashboard/src/components/ui/input.tsx
  - packages/dashboard/src/components/ui/textarea.tsx
  - packages/dashboard/src/components/ui/tooltip.tsx
  - packages/dashboard/src/components/ui/scroll-area.tsx
  - packages/dashboard/src/components/ui/card.tsx
  - packages/dashboard/src/components/ui/select.tsx
  - packages/dashboard/src/components/ui/separator.tsx
autonomous: true
requirements:
  - THME-01
  - THME-02

must_haves:
  truths:
    - "Every dashboard page uses bioluminescent color palette with zero residual slate-* hardcoded Tailwind classes"
    - "CSS variables from app.css are the single source of truth for all colors — changing a variable updates all pages"
    - "No visible 'brain' string appears anywhere in the dashboard UI"
    - "Empty states on graph view and approvals page use themed styling consistent with the deep-sea aesthetic"
  artifacts:
    - path: "packages/dashboard/src/routes/__root.tsx"
      provides: "Root layout with CSS variable background/text instead of slate-950/slate-100"
      contains: "var(--bg-void)"
    - path: "packages/dashboard/src/routes/approvals.tsx"
      provides: "Approvals page with themed empty states and heading"
      contains: "var(--text-primary)"
    - path: "packages/dashboard/src/components/activity-feed.tsx"
      provides: "Activity feed with CSS variable colors"
      contains: "var(--text-secondary)"
    - path: "packages/dashboard/src/components/quick-approve.tsx"
      provides: "Quick approve widget with CSS variable colors"
      contains: "var(--bg-surface)"
    - path: "packages/dashboard/src/components/ui/card.tsx"
      provides: "Card primitive with CSS variable border/bg"
      contains: "var(--bg-surface)"
  key_links:
    - from: "packages/dashboard/src/app.css"
      to: "all dashboard components"
      via: "CSS custom properties (--bg-void, --bg-surface, --text-primary, etc.)"
      pattern: "var\\(--"
---

<objective>
Replace all hardcoded slate-* Tailwind classes across the dashboard with CSS variable equivalents from app.css, and verify no "brain" terminology remains in the UI.

Purpose: Establishes the bioluminescent deep-sea visual foundation so all downstream phases (14-17) build on a consistent theme. Changing a CSS variable in app.css must update every page.

Output: All 15 files with slate-* classes migrated to CSS variables. Zero slate-* class references remaining in the dashboard source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/dashboard/src/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate application components from slate-* to CSS variables</name>
  <files>
    packages/dashboard/src/routes/__root.tsx,
    packages/dashboard/src/routes/approvals.tsx,
    packages/dashboard/src/components/activity-feed.tsx,
    packages/dashboard/src/components/quick-approve.tsx
  </files>
  <read_first>
    packages/dashboard/src/app.css,
    packages/dashboard/src/routes/__root.tsx,
    packages/dashboard/src/routes/approvals.tsx,
    packages/dashboard/src/components/activity-feed.tsx,
    packages/dashboard/src/components/quick-approve.tsx,
    packages/dashboard/src/components/sidebar.tsx
  </read_first>
  <action>
Replace every hardcoded slate-* Tailwind class with the equivalent CSS variable using inline style attributes (matching the pattern already established in sidebar.tsx, stat-card.tsx, approval-card.tsx).

The CSS variable mapping for all replacements:

| Tailwind class | CSS variable replacement |
|----------------|------------------------|
| bg-slate-950 | var(--bg-void) |
| bg-slate-900 | var(--bg-surface) |
| bg-slate-800 | var(--bg-elevated) |
| bg-slate-700 | var(--border-glow) |
| border-slate-700 | var(--border-subtle) |
| border-slate-600 | var(--border-glow) |
| text-slate-100 | var(--text-primary) |
| text-slate-300 | var(--text-secondary) |
| text-slate-400 | var(--text-secondary) |
| text-slate-500 | var(--text-muted) |

**__root.tsx** (1 occurrence):
- Line 6: Replace `bg-slate-950 text-slate-100` with inline style: `style={{ backgroundColor: 'var(--bg-void)', color: 'var(--text-primary)' }}`
- Keep the `flex h-screen` Tailwind classes (layout, not color)

**approvals.tsx** (6 occurrences):
- Line 25: `text-slate-100` -> style={{ color: 'var(--text-primary)' }}
- Line 26: `text-slate-500` -> style={{ color: 'var(--text-muted)' }}
- Line 38: `text-slate-100` -> style={{ color: 'var(--text-primary)' }}
- Line 39: `text-slate-500` -> style={{ color: 'var(--text-muted)' }}
- Line 48: `text-slate-100` -> style={{ color: 'var(--text-primary)' }}
- Line 52: `bg-slate-800` -> style={{ backgroundColor: 'var(--bg-elevated)' }}

**activity-feed.tsx** (8 occurrences):
- Line 21: `text-slate-100` -> style={{ color: 'var(--text-primary)' }}
- Line 22: `text-slate-500` -> style={{ color: 'var(--text-muted)' }}
- Line 42: Separator `bg-slate-800` -> style={{ backgroundColor: 'var(--bg-elevated)' }}
- Line 43: `text-slate-500` -> style={{ color: 'var(--text-muted)' }}
- Line 44: Separator `bg-slate-800` -> style={{ backgroundColor: 'var(--bg-elevated)' }}
- Line 53: Badge `bg-slate-700 border-slate-600 text-slate-300` -> style={{ backgroundColor: 'var(--border-glow)', borderColor: 'var(--border-glow)', color: 'var(--text-secondary)' }}
- Line 58: `text-slate-400` -> style={{ color: 'var(--text-secondary)' }}
- Line 59: `text-slate-500` -> style={{ color: 'var(--text-muted)' }}

**quick-approve.tsx** (8 occurrences):
- Line 13: `bg-slate-800` -> style={{ backgroundColor: 'var(--bg-elevated)' }}
- Line 24: `text-slate-100` -> style={{ color: 'var(--text-primary)' }}
- Line 25: `text-slate-500` -> style={{ color: 'var(--text-muted)' }}
- Line 40: `bg-slate-800 border border-slate-700` -> style={{ backgroundColor: 'var(--bg-surface)', border: '1px solid var(--border-subtle)' }}
- Line 43: `text-slate-100` -> style={{ color: 'var(--text-primary)' }}
- Line 46: `text-slate-400` -> style={{ color: 'var(--text-secondary)' }}
- Line 52: `hover:bg-slate-700` -> use style with onMouseEnter/onMouseLeave OR keep the emerald/red text colors and replace only `hover:bg-slate-700` with a CSS class. Simplest: remove `hover:bg-slate-700` from className and rely on the ghost variant's existing hover behavior since the button already has variant="ghost".
- Line 61: Same as line 52 for the reject button.

For the hover states on quick-approve buttons (lines 52, 61): Remove `hover:bg-slate-700` from the className. The `variant="ghost"` on Button already provides hover styling. Keep the `text-emerald-500 hover:text-emerald-400` and `text-red-500 hover:text-red-400` as-is — these are semantic action colors, not theme colors.

Also verify THME-02: Search all dashboard source for any remaining "brain" or "Brain" strings in user-visible text. The quick-260327-hr6 task already renamed these, so this should be a no-op confirmation. If any are found, replace with "Myco".
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/myco && grep -r "slate-" packages/dashboard/src/routes/__root.tsx packages/dashboard/src/routes/approvals.tsx packages/dashboard/src/components/activity-feed.tsx packages/dashboard/src/components/quick-approve.tsx; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "slate-" across __root.tsx, approvals.tsx, activity-feed.tsx, quick-approve.tsx returns zero matches
    - grep -rn "var(--" __root.tsx returns at least 1 match for var(--bg-void)
    - grep -rn "var(--" approvals.tsx returns at least 3 matches for var(--text-primary), var(--text-muted), var(--bg-elevated)
    - grep -rn "var(--" activity-feed.tsx returns at least 5 matches
    - grep -rn "var(--" quick-approve.tsx returns at least 5 matches
    - grep -ri "brain" packages/dashboard/src/ returns zero matches in user-visible strings
    - npm run build --workspace=packages/dashboard completes without errors
  </acceptance_criteria>
  <done>All 4 application files have zero slate-* classes and use CSS variables exclusively. No "brain" text in dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate shadcn/ui primitives from slate-* to CSS variables</name>
  <files>
    packages/dashboard/src/components/ui/card.tsx,
    packages/dashboard/src/components/ui/input.tsx,
    packages/dashboard/src/components/ui/textarea.tsx,
    packages/dashboard/src/components/ui/select.tsx,
    packages/dashboard/src/components/ui/tooltip.tsx,
    packages/dashboard/src/components/ui/scroll-area.tsx,
    packages/dashboard/src/components/ui/separator.tsx
  </files>
  <read_first>
    packages/dashboard/src/app.css,
    packages/dashboard/src/components/ui/card.tsx,
    packages/dashboard/src/components/ui/input.tsx,
    packages/dashboard/src/components/ui/textarea.tsx,
    packages/dashboard/src/components/ui/select.tsx,
    packages/dashboard/src/components/ui/tooltip.tsx,
    packages/dashboard/src/components/ui/scroll-area.tsx,
    packages/dashboard/src/components/ui/separator.tsx
  </read_first>
  <action>
Replace slate-* Tailwind classes in shadcn/ui primitives with Tailwind classes that reference CSS custom properties. For UI primitives, prefer adding custom Tailwind utility classes in app.css (or using arbitrary value syntax like `bg-[var(--bg-surface)]`) rather than inline styles, since these components use the `cn()` merge pattern where inline styles could conflict with consumer overrides.

Use Tailwind arbitrary value syntax for all replacements:

| Tailwind slate class | Replacement (arbitrary value) |
|---------------------|-------------------------------|
| bg-slate-950 | bg-[var(--bg-void)] |
| bg-slate-900 | bg-[var(--bg-surface)] |
| bg-slate-800 | bg-[var(--bg-elevated)] |
| bg-slate-700 | bg-[var(--border-glow)] |
| border-slate-700 | border-[var(--border-subtle)] |
| border-slate-600 | border-[var(--border-glow)] |
| text-slate-100 | text-[var(--text-primary)] |
| text-slate-300 | text-[var(--text-secondary)] |
| text-slate-400 | text-[var(--text-secondary)] |
| text-slate-500 | text-[var(--text-muted)] |
| focus-visible:ring-indigo-500 | focus-visible:ring-[var(--glow-teal)] |
| focus:ring-indigo-500 | focus:ring-[var(--glow-teal)] |
| focus:bg-slate-800 | focus:bg-[var(--bg-elevated)] |
| focus:text-slate-100 | focus:text-[var(--text-primary)] |

**card.tsx** (2 occurrences):
- Line 11: `border-slate-700 bg-slate-800 text-slate-100` -> `border-[var(--border-subtle)] bg-[var(--bg-elevated)] text-[var(--text-primary)]`
- Line 49: `text-slate-400` -> `text-[var(--text-secondary)]`

**input.tsx** (1 occurrence, multiple classes on one line):
- Line 13: Replace `border-slate-700 bg-slate-900 text-slate-100 placeholder:text-slate-500 focus-visible:ring-indigo-500` with `border-[var(--border-subtle)] bg-[var(--bg-surface)] text-[var(--text-primary)] placeholder:text-[var(--text-muted)] focus-visible:ring-[var(--glow-teal)]`

**textarea.tsx** (1 occurrence):
- Line 12: Same pattern as input.tsx: `border-slate-700 bg-slate-900 text-slate-100 placeholder:text-slate-500 focus-visible:ring-indigo-500` -> `border-[var(--border-subtle)] bg-[var(--bg-surface)] text-[var(--text-primary)] placeholder:text-[var(--text-muted)] focus-visible:ring-[var(--glow-teal)]`

**select.tsx** (5 occurrences):
- Line 17 (SelectTrigger): `border-slate-700 bg-slate-900 text-slate-100 placeholder:text-slate-500 focus:ring-indigo-500` -> `border-[var(--border-subtle)] bg-[var(--bg-surface)] text-[var(--text-primary)] placeholder:text-[var(--text-muted)] focus:ring-[var(--glow-teal)]`
- Line 73 (SelectContent): `border-slate-700 bg-slate-900 text-slate-100` -> `border-[var(--border-subtle)] bg-[var(--bg-surface)] text-[var(--text-primary)]`
- Line 116 (SelectItem): `focus:bg-slate-800 focus:text-slate-100` -> `focus:bg-[var(--bg-elevated)] focus:text-[var(--text-primary)]`
- Line 137 (SelectSeparator): `bg-slate-800` -> `bg-[var(--bg-elevated)]`

**tooltip.tsx** (1 occurrence):
- Line 18: `bg-slate-800 text-slate-100` -> `bg-[var(--bg-elevated)] text-[var(--text-primary)]`

**scroll-area.tsx** (1 occurrence):
- Line 40: `bg-slate-700` -> `bg-[var(--border-glow)]`

**separator.tsx** (1 occurrence):
- Line 18: `bg-slate-800` -> `bg-[var(--bg-elevated)]`

After all replacements, run the dashboard build to confirm Tailwind processes the arbitrary value syntax correctly.
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/myco && grep -r "slate-" packages/dashboard/src/components/ui/ && echo "FAIL: slate classes remain" || echo "PASS: no slate classes" && npm run build --workspace=packages/dashboard 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "slate-" packages/dashboard/src/components/ui/ returns zero matches
    - grep -r "slate-" packages/dashboard/src/ returns zero matches total (combined with Task 1)
    - grep -c "var(--" packages/dashboard/src/components/ui/card.tsx returns at least 2
    - grep -c "var(--" packages/dashboard/src/components/ui/input.tsx returns at least 1
    - grep -c "var(--" packages/dashboard/src/components/ui/select.tsx returns at least 4
    - npm run build --workspace=packages/dashboard exits with code 0
  </acceptance_criteria>
  <done>All 7 UI primitive files have zero slate-* classes. The entire packages/dashboard/src/ directory has zero slate-* Tailwind color classes. Dashboard builds successfully.</done>
</task>

</tasks>

<verification>
Final phase-level verification:

1. Zero slate-* classes: `grep -r "slate-" packages/dashboard/src/` must return no matches
2. Zero "brain" strings: `grep -ri "brain" packages/dashboard/src/` must return no matches in user-visible text
3. CSS variables used everywhere: `grep -rc "var(--" packages/dashboard/src/ | grep -v ":0$" | wc -l` should show broad adoption
4. Build passes: `npm run build --workspace=packages/dashboard` exits cleanly
5. Spot check: Open dashboard in browser — all pages should render with the dark void background, teal accents, and no broken/unstyled elements
</verification>

<success_criteria>
- Zero occurrences of `slate-*` color classes in any file under `packages/dashboard/src/`
- Zero occurrences of "brain" or "Brain" in user-visible dashboard text
- All colors flow from CSS variables defined in `app.css` — changing `--bg-void` would change every page's background
- Dashboard builds without errors
- All 4 dashboard pages (home, graph, approvals, activity feed) render with consistent bioluminescent styling
</success_criteria>

<output>
After completion, create `.planning/phases/13-theme-language-foundation/13-01-SUMMARY.md`
</output>

---
phase: 06-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/core/package.json
  - packages/mcp-server/package.json
  - packages/api-server/package.json
  - packages/dashboard/package.json
  - packages/core/src/db.ts
  - packages/mcp-server/src/index.ts
  - packages/mcp-server/src/cli.ts
  - packages/mcp-server/src/tools.ts
  - packages/mcp-server/src/consolidator.ts
  - packages/api-server/src/db.ts
  - packages/api-server/src/routes/approvals.ts
  - packages/api-server/src/routes/dashboard.ts
  - packages/api-server/src/routes/episodes.ts
  - packages/api-server/src/routes/entities.ts
autonomous: true
requirements: [REN-01, REN-02, REN-03, REN-04, REN-05]

must_haves:
  truths:
    - "All four package.json files use @myco/* package names"
    - "Cross-package imports resolve to @myco/core"
    - "MCP server binary is 'myco' and CLI binary is 'myco-cli'"
    - "Default database path uses ~/.local/share/myco/"
    - "MCP server identifies as 'myco' not 'ai-workbots-brain'"
    - "Tool descriptions reference 'Myco' not 'brain'"
    - "BRAIN_DB_PATH is still accepted as fallback, with MYCO_DB_PATH as primary"
  artifacts:
    - path: "package.json"
      provides: "Root workspace package name"
      contains: "myco"
    - path: "packages/core/package.json"
      provides: "@myco/core package identity"
      contains: "@myco/core"
    - path: "packages/mcp-server/package.json"
      provides: "@myco/mcp-server package identity with myco/myco-cli binaries"
      contains: "@myco/mcp-server"
    - path: "packages/api-server/package.json"
      provides: "@myco/api-server package identity"
      contains: "@myco/api-server"
    - path: "packages/dashboard/package.json"
      provides: "@myco/dashboard package identity"
      contains: "@myco/dashboard"
    - path: "packages/core/src/db.ts"
      provides: "Database path using myco directory with BRAIN_DB_PATH fallback"
      contains: "myco"
    - path: "packages/mcp-server/src/index.ts"
      provides: "MCP server named myco"
      contains: "name: 'myco'"
  key_links:
    - from: "packages/mcp-server/src/index.ts"
      to: "@myco/core"
      via: "import statement"
      pattern: "from '@myco/core'"
    - from: "packages/api-server/src/db.ts"
      to: "@myco/core"
      via: "import statement"
      pattern: "from '@myco/core'"
    - from: "packages/core/src/db.ts"
      to: "~/.local/share/myco/brain.db"
      via: "getDefaultDbPath function"
      pattern: "myco.*brain\\.db"
---

<objective>
Rename all package identities, imports, binaries, database paths, and MCP server strings from ai-workbots/brain to myco.

Purpose: Core mechanical rename across all four packages so the codebase identifies as Myco everywhere.
Output: All source files updated with new names; cross-package imports use @myco/* namespace.
</objective>

<execution_context>
@/Users/olive/.claude/get-shit-done/workflows/execute-plan.md
@/Users/olive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rename/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package identities and cross-package imports</name>
  <files>
    package.json,
    packages/core/package.json,
    packages/mcp-server/package.json,
    packages/api-server/package.json,
    packages/dashboard/package.json,
    packages/mcp-server/src/consolidator.ts,
    packages/mcp-server/src/tools.ts,
    packages/mcp-server/src/index.ts,
    packages/mcp-server/src/cli.ts,
    packages/api-server/src/db.ts,
    packages/api-server/src/routes/approvals.ts,
    packages/api-server/src/routes/dashboard.ts,
    packages/api-server/src/routes/episodes.ts,
    packages/api-server/src/routes/entities.ts
  </files>
  <read_first>
    package.json,
    packages/core/package.json,
    packages/mcp-server/package.json,
    packages/api-server/package.json,
    packages/dashboard/package.json,
    packages/mcp-server/src/consolidator.ts,
    packages/mcp-server/src/tools.ts,
    packages/mcp-server/src/index.ts,
    packages/mcp-server/src/cli.ts,
    packages/api-server/src/db.ts,
    packages/api-server/src/routes/approvals.ts,
    packages/api-server/src/routes/dashboard.ts,
    packages/api-server/src/routes/episodes.ts,
    packages/api-server/src/routes/entities.ts
  </read_first>
  <action>
    Perform the following renames across all listed files:

    **Root package.json:**
    - Change `"name": "ai-workbots"` to `"name": "myco"`

    **All 4 packages/*/package.json:**
    - `@ai-workbots/core` -> `@myco/core`
    - `@ai-workbots/mcp-server` -> `@myco/mcp-server`
    - `@ai-workbots/api-server` -> `@myco/api-server`
    - `@ai-workbots/dashboard` -> `@myco/dashboard`
    - In mcp-server/package.json bin field: `"brain-mcp": "./dist/index.js"` -> `"myco": "./dist/index.js"`
    - In mcp-server/package.json bin field: `"brain-cli": "./dist/cli.js"` -> `"myco-cli": "./dist/cli.js"`
    - In mcp-server/package.json dependencies: `"@ai-workbots/core": "*"` -> `"@myco/core": "*"`
    - In api-server/package.json dependencies: `"@ai-workbots/core": "*"` -> `"@myco/core": "*"`

    **All source files with imports:**
    - Every `from '@ai-workbots/core'` -> `from '@myco/core'`
    - Every `import type { ... } from '@ai-workbots/core'` -> `import type { ... } from '@myco/core'`
    - This affects: consolidator.ts, tools.ts, index.ts, cli.ts, api-server/db.ts, and all api-server route files

    **packages/mcp-server/src/index.ts:**
    - Change `name: 'ai-workbots-brain'` to `name: 'myco'`
    - Change `console.error('ai-workbots-brain MCP server started')` to `console.error('myco MCP server started')`

    **packages/mcp-server/src/cli.ts:**
    - Change all `brain-cli` references to `myco-cli` in:
      - JSDoc comment at top: `brain-cli:` -> `myco-cli:`
      - Usage string: `Usage: brain-cli <command>` -> `Usage: myco-cli <command>`
      - All console.error/log prefixes: `[brain-cli]` -> `[myco-cli]`
      - Usage error messages: `Usage: brain-cli resolve-approval` -> `Usage: myco-cli resolve-approval`

    **packages/mcp-server/src/tools.ts:**
    - Change `'Store a piece of knowledge in the brain.'` to `'Store a piece of knowledge in Myco.'`
    - Change `'Retrieve relevant knowledge from the brain using natural language'` to `'Retrieve relevant knowledge from Myco using natural language'`
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/ai-workbots && grep -r "@ai-workbots" packages/*/package.json packages/*/src/ --include="*.ts" --include="*.json" | grep -v node_modules | grep -v dist | grep -v ".planning" && echo "FAIL: old references found" || echo "PASS: no old references in source"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"name": "@myco/core"' packages/core/package.json returns 1
    - grep -c '"name": "@myco/mcp-server"' packages/mcp-server/package.json returns 1
    - grep -c '"name": "@myco/api-server"' packages/api-server/package.json returns 1
    - grep -c '"name": "@myco/dashboard"' packages/dashboard/package.json returns 1
    - grep -c '"name": "myco"' package.json returns 1
    - grep -c '"myco":' packages/mcp-server/package.json returns 1
    - grep -c '"myco-cli":' packages/mcp-server/package.json returns 1
    - grep -c "brain-mcp" packages/mcp-server/package.json returns 0
    - grep -c "brain-cli" packages/mcp-server/package.json returns 0
    - grep -c "@ai-workbots" packages/mcp-server/src/tools.ts returns 0
    - grep -c "name: 'myco'" packages/mcp-server/src/index.ts returns 1
    - grep "from '@myco/core'" packages/mcp-server/src/consolidator.ts succeeds
    - grep "from '@myco/core'" packages/api-server/src/db.ts succeeds
    - grep "in Myco" packages/mcp-server/src/tools.ts succeeds
    - Zero occurrences of @ai-workbots in any packages/*/src/*.ts file
  </acceptance_criteria>
  <done>All package.json files use @myco/* names, bin entries are myco/myco-cli, all cross-package imports use @myco/core, MCP server name is 'myco', CLI strings say myco-cli, tool descriptions say Myco.</done>
</task>

<task type="auto">
  <name>Task 2: Update database path and environment variable</name>
  <files>
    packages/core/src/db.ts
  </files>
  <read_first>
    packages/core/src/db.ts
  </read_first>
  <action>
    Update the database path logic in packages/core/src/db.ts:

    1. In `getDefaultDbPath()`: Change `path.join(xdgData, 'ai-workbots', 'brain.db')` to `path.join(xdgData, 'myco', 'brain.db')`
       - The database filename stays `brain.db` (no reason to rename the file itself)
       - Only the directory changes from `ai-workbots` to `myco`

    2. In `openDatabase()`: Update the env var resolution to check MYCO_DB_PATH first, falling back to BRAIN_DB_PATH for existing users:
       - Current: `const resolvedPath = dbPath ?? process.env.BRAIN_DB_PATH ?? getDefaultDbPath();`
       - New: `const resolvedPath = dbPath ?? process.env.MYCO_DB_PATH ?? process.env.BRAIN_DB_PATH ?? getDefaultDbPath();`
       - This ensures existing users with BRAIN_DB_PATH set are not broken
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/ai-workbots && grep "myco" packages/core/src/db.ts && grep "MYCO_DB_PATH" packages/core/src/db.ts && grep "BRAIN_DB_PATH" packages/core/src/db.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "'myco'" packages/core/src/db.ts returns at least 1 (directory name in path)
    - grep -c "MYCO_DB_PATH" packages/core/src/db.ts returns at least 1
    - grep -c "BRAIN_DB_PATH" packages/core/src/db.ts returns at least 1 (fallback preserved)
    - grep -c "ai-workbots" packages/core/src/db.ts returns 0
    - The line reads: process.env.MYCO_DB_PATH ?? process.env.BRAIN_DB_PATH ?? getDefaultDbPath()
  </acceptance_criteria>
  <done>Default DB path is ~/.local/share/myco/brain.db, MYCO_DB_PATH is primary env var, BRAIN_DB_PATH preserved as fallback.</done>
</task>

</tasks>

<verification>
Run: `cd /Users/olive/Documents/GitHub/ai-workbots && grep -rn "ai-workbots\|@ai-workbots" packages/*/package.json packages/*/src/**/*.ts package.json | grep -v node_modules | grep -v dist`
Expected: Zero matches.

Run: `grep -c "brain-mcp\|brain-cli" packages/mcp-server/package.json`
Expected: 0

Run: `grep "MYCO_DB_PATH" packages/core/src/db.ts`
Expected: Present in env var check.
</verification>

<success_criteria>
All package names are @myco/*, all imports use @myco/core, binaries are myco and myco-cli, DB path uses myco directory, MYCO_DB_PATH is primary env var with BRAIN_DB_PATH fallback, MCP server name is 'myco', tool descriptions reference Myco.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rename/06-01-SUMMARY.md`
</output>

---
phase: 10-prepared-statements
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - packages/api-server/src/db.ts
  - packages/api-server/src/index.ts
  - packages/api-server/src/routes/approvals.ts
  - packages/api-server/src/routes/dashboard.ts
  - packages/api-server/src/routes/entities.ts
  - packages/api-server/src/routes/episodes.ts
  - packages/api-server/src/routes/graph.ts
autonomous: true
requirements:
  - STMT-01
  - STMT-02

must_haves:
  truths:
    - "All API route handlers use pre-compiled statements instead of inline db.prepare()"
    - "The API server initializes prepared statements once at startup"
    - "No db.prepare() call exists inside any API route handler function"
    - "All API endpoints return the same response shapes as before (no behavioral regression)"
  artifacts:
    - path: "packages/api-server/src/db.ts"
      provides: "Singleton DB + prepared statements initialization"
      exports: ["getDb", "getStatements"]
    - path: "packages/api-server/src/routes/dashboard.ts"
      provides: "Dashboard route using prepared statements"
      contains: "stmts"
  key_links:
    - from: "packages/api-server/src/index.ts"
      to: "packages/api-server/src/db.ts"
      via: "getStatements() called alongside getDb()"
      pattern: "getStatements\\(\\)"
    - from: "packages/api-server/src/routes/approvals.ts"
      to: "packages/core/src/statements.ts"
      via: "MycoStatements type parameter"
      pattern: "MycoStatements"
---

<objective>
Refactor all API server route files to use the prepared statement factory created in Plan 01, eliminating per-request db.prepare() calls from every API endpoint.

Purpose: Complete the prepared statement migration across both server packages. The API server handles dashboard, entity, episode, approval, and graph queries — all hot paths that benefit from pre-compiled statements.

Output: All API route files refactored, db.ts updated to expose statements, no inline db.prepare() in any route handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-prepared-statements/10-CONTEXT.md
@.planning/phases/10-prepared-statements/10-01-SUMMARY.md

@packages/core/src/statements.ts
@packages/api-server/src/db.ts
@packages/api-server/src/index.ts
@packages/api-server/src/routes/approvals.ts
@packages/api-server/src/routes/dashboard.ts
@packages/api-server/src/routes/entities.ts
@packages/api-server/src/routes/episodes.ts
@packages/api-server/src/routes/graph.ts

<interfaces>
<!-- From packages/core/src/statements.ts (created in Plan 01) -->
<!-- Executor: read the actual file — this is a preview of the expected exports -->
export interface MycoStatements {
  // Entity
  selectEntityByNameType: Statement;
  insertEntity: Statement;
  selectAllEntityNames: Statement;
  deleteEntityById: Statement;
  updateEntityTimestampConfidence: Statement;
  // Observation
  insertObservation: Statement;
  insertObservationWithEmbeddingFlag: Statement;
  flagObservationNeedsEmbedding: Statement;
  clearObservationEmbeddingFlag: Statement;
  selectPendingEmbeddings: Statement;
  selectObservationsByEntityId: Statement;
  updateObservationEntityId: Statement;
  // Relationship
  insertRelationship: Statement;
  selectRelationshipExists: Statement;
  updateRelationshipFromId: Statement;
  updateRelationshipToId: Statement;
  // Embedding/vector
  insertVecEmbedding: Statement;
  knnSearchObservations: Statement;
  knnSearchForContradiction: Statement;
  knnSearchForRelationships: Statement;
  // FTS
  insertFtsObservation: Statement;
  ftsSearchObservations: Statement;
  ftsSearchEntityMentions: Statement;
  // Episode
  insertEpisode: Statement;
  selectUnconsolidatedEpisodes: Statement;
  // Approval queue
  selectPendingApprovals: Statement;
  selectApprovalById: Statement;
  updateApprovalStatus: Statement;
  insertApprovalQueueItem: Statement;
}

export function prepareStatements(db: Database.Database): MycoStatements;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API server db.ts and index.ts to initialize statements</name>
  <files>packages/api-server/src/db.ts, packages/api-server/src/index.ts</files>
  <read_first>
    - packages/core/src/statements.ts (factory API — read the actual file, not the interface preview)
    - packages/api-server/src/db.ts (current singleton pattern)
    - packages/api-server/src/index.ts (current startup and route mounting)
  </read_first>
  <action>
    **db.ts changes:**
    Add a `getStatements()` function that lazily creates and caches the statements object:
    ```typescript
    import { openDatabase, prepareStatements } from '@myco/core';
    import type { MycoStatements } from '@myco/core';
    import type Database from 'better-sqlite3';

    let _db: Database.Database | null = null;
    let _stmts: MycoStatements | null = null;

    export function getDb(): Database.Database {
      if (!_db) {
        _db = openDatabase();
      }
      return _db;
    }

    export function getStatements(): MycoStatements {
      if (!_stmts) {
        _stmts = prepareStatements(getDb());
      }
      return _stmts;
    }
    ```

    **index.ts changes:**
    After `const db = getDb();` add `const stmts = getStatements();`
    Update all route mounting calls to pass stmts:
    - `dashboardRoutes(db)` becomes `dashboardRoutes(db, stmts)`
    - `approvalsRoutes(db)` becomes `approvalsRoutes(db, stmts)`
    - `entitiesRoutes(db)` becomes `entitiesRoutes(db, stmts)`
    - `episodesRoutes(db)` becomes `episodesRoutes(db, stmts)`
    - `graphRoutes(db)` becomes `graphRoutes(db, stmts)`

    Import `getStatements` from `./db.js`.
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/myco && npx tsc --noEmit -p packages/api-server/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "getStatements" packages/api-server/src/db.ts returns at least 1
    - grep -c "prepareStatements" packages/api-server/src/db.ts returns at least 1
    - grep -c "getStatements" packages/api-server/src/index.ts returns at least 1
    - grep "stmts" packages/api-server/src/index.ts shows stmts passed to all route factories
  </acceptance_criteria>
  <done>API server initializes prepared statements once at startup and passes them to all route factories.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all API route files to use prepared statements</name>
  <files>packages/api-server/src/routes/approvals.ts, packages/api-server/src/routes/dashboard.ts, packages/api-server/src/routes/entities.ts, packages/api-server/src/routes/episodes.ts, packages/api-server/src/routes/graph.ts</files>
  <read_first>
    - packages/core/src/statements.ts (actual factory — know exact statement names)
    - packages/api-server/src/routes/approvals.ts (heaviest file — ~15 inline prepare calls)
    - packages/api-server/src/routes/dashboard.ts (~10 inline prepare calls)
    - packages/api-server/src/routes/entities.ts (~4 inline prepare calls)
    - packages/api-server/src/routes/graph.ts (~2 inline prepare calls)
    - packages/api-server/src/routes/episodes.ts (~1 inline prepare call)
  </read_first>
  <action>
    **All route files:** Change factory function signatures to accept stmts as second parameter:
    - `dashboardRoutes(db: Database.Database, stmts: MycoStatements): Hono`
    - `approvalsRoutes(db: Database.Database, stmts: MycoStatements): Hono`
    - `entitiesRoutes(db: Database.Database, stmts: MycoStatements): Hono`
    - `episodesRoutes(db: Database.Database, stmts: MycoStatements): Hono`
    - `graphRoutes(db: Database.Database, stmts: MycoStatements): Hono`

    Import `MycoStatements` from `@myco/core`.

    **dashboard.ts** — Replace all inline db.prepare() with stmts:
    The dashboard route has many single-purpose COUNT queries and aggregate queries. Some of these are unique to the dashboard and may not exist in the factory yet (e.g., `SELECT COUNT(*) as n FROM entities`, top-connected entities query, type breakdown, growth stats).

    For statements NOT in the factory: The executor must add them to `packages/core/src/statements.ts` first. Add these dashboard-specific statements:
    - `countPendingApprovals`: `SELECT COUNT(*) as n FROM approval_queue WHERE status = 'pending'`
    - `countEntities`: `SELECT COUNT(*) as n FROM entities`
    - `countRelationships`: `SELECT COUNT(*) as n FROM relationships`
    - `countObservations`: `SELECT COUNT(*) as n FROM observations`
    - `selectRecentEpisodes`: `SELECT id, session_id, agent_id, event_type, created_at FROM episodes ORDER BY created_at DESC LIMIT 20`
    - `selectTopConnected`: The top-connected entities query with subquery for connection_count
    - `selectTypeBreakdown`: `SELECT type, COUNT(*) as count FROM entities GROUP BY type ORDER BY count DESC`
    - `countEntitiesAfter`: `SELECT COUNT(*) as n FROM entities WHERE created_at > ?`
    - `countObservationsAfter`: `SELECT COUNT(*) as n FROM observations WHERE created_at > ?`
    - `countRelationshipsAfter`: `SELECT COUNT(*) as n FROM relationships WHERE created_at > ?`

    Also add for other routes:
    - `selectAllPendingApprovals`: `SELECT * FROM approval_queue WHERE status = 'pending' ORDER BY created_at DESC LIMIT 50`
    - `selectEntitiesPaginated`: `SELECT id, name, type, confidence, created_at FROM entities ORDER BY updated_at DESC LIMIT ? OFFSET ?`
    - `selectEntityById`: `SELECT * FROM entities WHERE id = ?`
    - `selectObservationsByEntity`: `SELECT * FROM observations WHERE entity_id = ? ORDER BY created_at DESC`
    - `selectConnectedEntities`: The joined query for connected entities
    - `selectGraphNodes`: The graph node query with obs_count subquery
    - `selectGraphRelationships`: `SELECT id, from_id, to_id, type, confidence, source_type, created_at FROM relationships`
    - `selectEpisodesPaginated`: `SELECT id, session_id, agent_id, event_type, payload, created_at FROM episodes ORDER BY created_at DESC LIMIT ?`

    Add all these to `MycoStatements` interface and `prepareStatements()` factory body.
    Then replace every inline db.prepare() call in each route file with the corresponding stmts property.

    **approvals.ts** — This is the most complex route file:
    - GET / handler: use `stmts.selectAllPendingApprovals`
    - PATCH /:id handler: use `stmts.selectApprovalById`, `stmts.updateApprovalStatus`
    - Merge candidate logic: use `stmts.selectEntityByNameType`, `stmts.updateObservationEntityId`, `stmts.updateRelationshipFromId`, `stmts.updateRelationshipToId`, `stmts.deleteEntityById`
    - Entity upsert: use `stmts.selectEntityByNameType`, `stmts.updateEntityTimestampConfidence`, `stmts.insertEntity` (the full form with explicit columns for api-server provenance)
    - Observation insert: use `stmts.insertObservationWithEmbeddingFlag`
    - FTS insert: use `stmts.insertFtsObservation`
    - Related entity handling: use `stmts.selectEntityByNameType`, `stmts.insertEntity`, `stmts.insertRelationship`
    - Final status update: use `stmts.updateApprovalStatus`

    NOTE: The approvals route uses slightly different INSERT statements than tools.ts — it hardcodes 'api-server' as session_id/agent_id. The prepared statement uses `?` placeholders, so the caller just passes 'api-server' as the parameter value. This is fine.

    **entities.ts:**
    - GET / handler: use `stmts.selectEntitiesPaginated`
    - GET /:id handler: use `stmts.selectEntityById`, `stmts.selectObservationsByEntity`, `stmts.selectConnectedEntities`

    **graph.ts:**
    - GET / handler: use `stmts.selectGraphNodes`, `stmts.selectGraphRelationships`

    **episodes.ts:**
    - GET / handler: use `stmts.selectEpisodesPaginated`

    CRITICAL: Do NOT reference `project` column in any new statement. Keep all SQL identical to current inline queries.
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/myco && npx tsc --noEmit -p packages/api-server/tsconfig.json && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "db\.prepare(" packages/api-server/src/routes/ returns 0 lines (all inline prepare eliminated)
    - grep -rn "db\.prepare(" packages/api-server/src/index.ts returns 0 lines
    - grep -c "stmts\." packages/api-server/src/routes/dashboard.ts is at least 10 (was 10 inline prepares)
    - grep -c "stmts\." packages/api-server/src/routes/approvals.ts is at least 10
    - grep -c "stmts\." packages/api-server/src/routes/entities.ts is at least 3
    - grep -c "MycoStatements" packages/api-server/src/routes/dashboard.ts returns at least 1
    - npx tsc --noEmit -p packages/api-server/tsconfig.json exits 0
    - npx vitest run exits 0
    - No occurrence of "project" in any new SQL statement (grep -c "project" packages/core/src/statements.ts still returns 0)
  </acceptance_criteria>
  <done>All API route handlers use prepared statements. Zero inline db.prepare() calls remain in any route file. All tests pass. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```bash
cd /Users/olive/Documents/GitHub/myco && npx vitest run
```

Verify zero inline db.prepare() in all production source (except documented exceptions):
```bash
grep -rn "db\.prepare(" packages/mcp-server/src/ packages/api-server/src/ packages/core/src/ --include="*.ts" | grep -v "node_modules" | grep -v "\.test\." | grep -v "\.d\."
```
Expected output: ONLY queryEntities and markBatchConsolidated exceptions (2-3 lines total).

TypeScript compilation check:
```bash
npx tsc --noEmit -p packages/core/tsconfig.json
npx tsc --noEmit -p packages/mcp-server/tsconfig.json
npx tsc --noEmit -p packages/api-server/tsconfig.json
```
</verification>

<success_criteria>
1. Zero `db.prepare()` calls in any API route handler function
2. `getStatements()` in api-server/db.ts lazily initializes and caches the statements object
3. All API endpoints return identical response shapes as before
4. TypeScript compiles cleanly across all packages
5. Full test suite passes
6. No reference to `project` column in any prepared statement
</success_criteria>

<output>
After completion, create `.planning/phases/10-prepared-statements/10-02-SUMMARY.md`
</output>

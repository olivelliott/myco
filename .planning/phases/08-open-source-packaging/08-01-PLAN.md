---
phase: 08-open-source-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - README.md
autonomous: true
requirements:
  - OSS-01
  - OSS-02

must_haves:
  truths:
    - "An Apache 2.0 LICENSE file exists in the repo root with 2026 year and correct author"
    - "README.md describes Myco as a persistent memory layer for Claude Code agents"
    - "README.md contains an ASCII architecture diagram showing the 4-package stack"
    - "README.md has a working quick-start section with install, build, and MCP setup commands"
    - "README.md documents all 7 MCP tools in a reference table"
    - "README.md uses the tagline 'Myco — your knowledge web.'"
  artifacts:
    - path: "LICENSE"
      provides: "Apache 2.0 full license text"
      contains: "Apache License"
    - path: "README.md"
      provides: "Primary project documentation"
      min_lines: 150
  key_links:
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "markdown link"
      pattern: "CONTRIBUTING.md"
    - from: "README.md"
      to: "LICENSE"
      via: "license badge or reference"
      pattern: "Apache.2\\.0|LICENSE"
---

<objective>
Create the LICENSE file and README.md for Myco's public GitHub release.

Purpose: These are the two highest-impact files for any open source project — LICENSE establishes legal terms, README is the front door for every visitor.
Output: LICENSE (Apache 2.0 full text) and README.md (complete project documentation).
</objective>

<execution_context>
@/Users/olive/.claude/get-shit-done/workflows/execute-plan.md
@/Users/olive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GETTING-STARTED.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Apache 2.0 LICENSE file</name>
  <files>LICENSE</files>
  <read_first>GETTING-STARTED.md</read_first>
  <action>
Create the file `LICENSE` in the repo root containing the standard Apache License, Version 2.0 full text.

In the copyright line at the top (before the license body), use:

```
Copyright 2026 Olive
```

Use the standard Apache 2.0 full license text from https://www.apache.org/licenses/LICENSE-2.0.txt — this is the canonical version. Do NOT abbreviate or modify the license body. The file should be plain text, no markdown formatting.
  </action>
  <verify>
    <automated>grep -q "Apache License" LICENSE && grep -q "Version 2.0" LICENSE && grep -q "2026" LICENSE && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- grep "Apache License" LICENSE returns a match
- grep "Version 2.0" LICENSE returns a match
- grep "2026" LICENSE returns a match
- File has no markdown formatting (no # headings, no ```blocks)
  </acceptance_criteria>
  <done>Apache 2.0 LICENSE file exists in repo root with correct year and author</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md</name>
  <files>README.md</files>
  <read_first>GETTING-STARTED.md, packages/mcp-server/src/tools.ts</read_first>
  <action>
Create `README.md` in the repo root. Use a developer-focused, technical, concise tone. No screenshots. The README replaces GETTING-STARTED.md as the primary entry point for the project.

**Exact section structure (in this order):**

1. **Title and tagline** — `# Myco` followed by `> Myco — your knowledge web.` then a one-paragraph description: "A persistent cognitive layer for Claude Code agents — an MCP server that gives every agent session access to long-term episodic memory, an open-schema knowledge graph, and entity relationships. A nightly deep sleep cycle consolidates raw episode logs into durable knowledge. Everything stays local."

2. **Features** (bulleted list) — Key capabilities:
   - Persistent memory across Claude Code sessions
   - Semantic search via local Ollama embeddings (nomic-embed-text, 768 dims)
   - Knowledge graph with entities, observations, and relationships
   - Nightly consolidation extracts structured facts from raw episodes
   - Human-in-the-loop approval queue for uncertain or contradictory findings
   - Visual dashboard (React 19 PWA) for graph exploration and approvals
   - SQLite + sqlite-vec — no external database, no cloud dependencies

3. **Architecture** — ASCII diagram. Reuse and adapt the architecture diagram from GETTING-STARTED.md (the box diagram showing Claude Code sessions -> MCP server -> brain.db -> api-server -> Dashboard PWA). Keep the same visual style.

4. **Quick Start** — Adapted from GETTING-STARTED.md. Sections:
   - Prerequisites (Ollama with nomic-embed-text + llama3.2, Node.js 22.x)
   - Clone and build (`git clone`, `npm install --legacy-peer-deps`, `npm run build`)
   - Add to Claude Code (show both settings.json edit and `claude mcp add` options — but use generic paths like `path/to/myco/packages/mcp-server/dist/index.js` not hardcoded /Users/olive paths)
   - Try it (3 quick example commands: remember, recall, query)
   - Dashboard (optional, `npm run api` + `cd packages/dashboard && npm run dev`)

5. **MCP Tools** — Reference table documenting all 7 tools. Use a table with columns: Tool | Description | Key Parameters. The 7 tools are:

   | Tool | Description | Key Parameters |
   |------|-------------|----------------|
   | `remember` | Store knowledge — creates entity with observation and optional relationships | `content`, `entity_name`, `entity_type`, `relations[]` |
   | `recall` | Semantic search over stored knowledge | `query`, `limit` |
   | `query` | Query knowledge graph by entity name, type, or relationship | `entity_name`, `entity_type`, `relation_type` |
   | `log_episode` | Log a session event for later consolidation | `event_type`, `payload` |
   | `consolidate` | Trigger the consolidation pipeline (extracts facts from episodes) | none |
   | `list_pending_approvals` | List items awaiting human review | `limit` |
   | `resolve_approval` | Approve, reject, or edit a queued item | `id`, `action`, `edited_content` |

6. **Project Structure** — Show the monorepo layout:
   ```
   packages/
     core/           # Shared SQLite schema, DB helpers, types
     mcp-server/     # MCP server (stdio transport)
     api-server/     # Hono REST API (port 3001)
     dashboard/      # React 19 PWA (Vite 8, Tailwind v4, shadcn/ui)
   ```

7. **Tech Stack** — Concise table (not the full CLAUDE.md stack doc). Key entries only:
   | Component | Technology |
   |-----------|------------|
   | Runtime | Node.js 22.x, TypeScript 5.9 |
   | MCP | @modelcontextprotocol/sdk |
   | Database | SQLite (better-sqlite3) + sqlite-vec |
   | Embeddings | Ollama (nomic-embed-text) |
   | API | Hono |
   | Dashboard | React 19, Vite 8, Tailwind v4, shadcn/ui |
   | Graph viz | react-force-graph-2d |

8. **Development** — Brief section:
   - `npm run dev` — run MCP server in dev mode
   - `npm run build` — build all packages
   - `npm test` — run tests (Vitest)
   - Link to CONTRIBUTING.md for full dev setup

9. **Environment Variables** — Table from GETTING-STARTED.md (MYCO_DB_PATH, OLLAMA_HOST, BRAIN_CONSOLIDATION_MODEL)

10. **License** — "Apache 2.0 — see [LICENSE](LICENSE)."

11. **Contributing** — "See [CONTRIBUTING.md](CONTRIBUTING.md) for development setup and guidelines."

Do NOT include: screenshots, badges (no CI yet), the "Known Issues" or "What Still Needs to Be Done" sections from GETTING-STARTED.md, personal paths (/Users/olive), or the "Using It Across Projects" section (too personal).
  </action>
  <verify>
    <automated>grep -q "# Myco" README.md && grep -q "your knowledge web" README.md && grep -q "remember" README.md && grep -q "recall" README.md && grep -q "consolidate" README.md && grep -q "Apache 2.0" README.md && grep -q "CONTRIBUTING.md" README.md && grep -c "^#" README.md | xargs test 5 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- grep "# Myco" README.md returns a match (title exists)
- grep "your knowledge web" README.md returns a match (tagline present)
- grep "remember" README.md returns a match (tool documented)
- grep "recall" README.md returns a match (tool documented)
- grep "query" README.md returns a match (tool documented)
- grep "log_episode" README.md returns a match (tool documented)
- grep "consolidate" README.md returns a match (tool documented)
- grep "list_pending_approvals" README.md returns a match (tool documented)
- grep "resolve_approval" README.md returns a match (tool documented)
- grep "Apache 2.0" README.md returns a match (license referenced)
- grep "CONTRIBUTING.md" README.md returns a match (contributing link)
- grep "Architecture" README.md returns a match (architecture section)
- grep "Quick Start" README.md returns a match (quick start section)
- File is at least 150 lines long
  </acceptance_criteria>
  <done>README.md exists with all 11 sections, documents all 7 MCP tools, uses the correct tagline, references LICENSE and CONTRIBUTING.md, contains architecture diagram, and has a working quick-start</done>
</task>

</tasks>

<verification>
- LICENSE file contains full Apache 2.0 text with "2026" and "Olive"
- README.md has title "# Myco", tagline "your knowledge web", all 7 MCP tools documented
- README.md contains no personal paths (/Users/olive)
- README.md links to CONTRIBUTING.md and LICENSE
- Both files are in repo root
</verification>

<success_criteria>
LICENSE and README.md exist in the repo root. README describes Myco accurately, documents all MCP tools, provides a working quick-start guide, and links to community files.
</success_criteria>

<output>
After completion, create `.planning/phases/08-open-source-packaging/08-01-SUMMARY.md`
</output>

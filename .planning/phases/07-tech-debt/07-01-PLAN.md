---
phase: 07-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api-server/src/routes/episodes.ts
  - packages/dashboard/src/lib/api.ts
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03]

must_haves:
  truths:
    - "/api/episodes returns { episodes: [...] } wrapper object, not a bare array"
    - "No dead fetchEpisodes export exists in the dashboard api.ts"
    - "Fresh-clone npm install + build succeeds with zero errors"
  artifacts:
    - path: "packages/api-server/src/routes/episodes.ts"
      provides: "Fixed episodes API response shape"
      contains: "episodes:"
    - path: "packages/dashboard/src/lib/api.ts"
      provides: "Clean API client with no dead exports"
  key_links:
    - from: "packages/api-server/src/routes/episodes.ts"
      to: "any future consumer of /api/episodes"
      via: "JSON response shape { episodes: [...] }"
      pattern: "c\\.json\\(\\{ episodes"
---

<objective>
Fix three known v1.0 tech debt items: the episodes API response shape mismatch, a dead fetchEpisodes export, and fresh-clone build verification.

Purpose: Clean up known defects before open source packaging so the codebase is correct and buildable from scratch.
Output: Two fixed source files and a verified clean build.
</objective>

<execution_context>
@/Users/olive/.claude/get-shit-done/workflows/execute-plan.md
@/Users/olive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current episodes route returns a bare array — must be wrapped -->
From packages/api-server/src/routes/episodes.ts:
```typescript
// Current (broken): c.json(parsed)  — returns EpisodeEntry[]
// Expected: c.json({ episodes: parsed }) — returns { episodes: EpisodeEntry[] }
```

From packages/dashboard/src/lib/api.ts:
```typescript
// Dead export — fetchEpisodes is never imported by any component
// The dashboard uses fetchDashboard() which returns recentEpisodes inline
export function fetchEpisodes(limit = 50): Promise<{ episodes: EpisodeEntry[] }>
export interface EpisodeEntry { id: string; session_id: string; agent_id: string; event_type: string; payload: string; created_at: string; consolidated_at: string | null; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix episodes API response shape and remove dead client export</name>
  <read_first>
    - packages/api-server/src/routes/episodes.ts
    - packages/dashboard/src/lib/api.ts
  </read_first>
  <files>packages/api-server/src/routes/episodes.ts, packages/dashboard/src/lib/api.ts</files>
  <action>
Two changes:

1. In `packages/api-server/src/routes/episodes.ts`, change line 28 from:
   `return c.json(parsed);`
   to:
   `return c.json({ episodes: parsed });`
   This wraps the response in the `{ episodes: [...] }` shape that any client would expect. No other changes needed in this file.

2. In `packages/dashboard/src/lib/api.ts`, delete the `EpisodeEntry` interface (lines 66-74) and the `fetchEpisodes` function (lines 115-121). These are dead code — no component imports them. The dashboard gets episodes via `fetchDashboard()` which returns `recentEpisodes` inline. Do NOT remove any other exports.
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/ai-workbots && grep -q 'episodes: parsed' packages/api-server/src/routes/episodes.ts && echo "DEBT-01: PASS" || echo "DEBT-01: FAIL"; grep -q 'fetchEpisodes' packages/dashboard/src/lib/api.ts && echo "DEBT-02: FAIL (still has fetchEpisodes)" || echo "DEBT-02: PASS"; grep -q 'EpisodeEntry' packages/dashboard/src/lib/api.ts && echo "DEBT-02: FAIL (still has EpisodeEntry)" || echo "DEBT-02: PASS (EpisodeEntry removed)"</automated>
  </verify>
  <acceptance_criteria>
    - grep "episodes: parsed" packages/api-server/src/routes/episodes.ts returns a match
    - grep "fetchEpisodes" packages/dashboard/src/lib/api.ts returns NO match
    - grep "EpisodeEntry" packages/dashboard/src/lib/api.ts returns NO match
  </acceptance_criteria>
  <done>
    - /api/episodes returns `{ episodes: [...] }` wrapper (DEBT-01)
    - Dead fetchEpisodes and EpisodeEntry removed from api.ts (DEBT-02)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify fresh-clone build succeeds</name>
  <read_first>
    - package.json
  </read_first>
  <files></files>
  <action>
Run the full build pipeline to verify everything compiles cleanly after the changes:

1. Delete all `node_modules` directories and `tsconfig.tsbuildinfo` files across the workspace to simulate a fresh clone:
   `find . -name node_modules -type d -prune -exec rm -rf {} + 2>/dev/null; find . -name '*.tsbuildinfo' -delete 2>/dev/null`

2. Run `npm install --legacy-peer-deps` from the repo root.

3. Run `npm run build` from the repo root.

Both commands must exit 0 with no errors. Warnings are acceptable. If the build fails, diagnose and fix the issue — the most likely cause is a missing import after removing EpisodeEntry, but no component imports it so this should be clean.
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/ai-workbots && npm run build 2>&1 | tail -5; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits with code 0
    - No TypeScript compilation errors in output
  </acceptance_criteria>
  <done>
    - Fresh install + build completes with no errors (DEBT-03)
  </done>
</task>

</tasks>

<verification>
All three DEBT requirements verified:
- DEBT-01: `curl /api/episodes` returns `{ "episodes": [...] }` shape (verified by grep on source)
- DEBT-02: `fetchEpisodes` and `EpisodeEntry` no longer in api.ts (verified by grep absence)
- DEBT-03: `npm install --legacy-peer-deps && npm run build` exits 0 (verified by build task)
</verification>

<success_criteria>
1. episodes.ts wraps response in `{ episodes: parsed }`
2. api.ts has no fetchEpisodes function or EpisodeEntry interface
3. Full workspace build succeeds from clean state
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt/07-01-SUMMARY.md`
</output>

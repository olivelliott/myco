---
phase: 06-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/vite.config.ts
  - packages/dashboard/index.html
  - packages/dashboard/src/routes/index.tsx
  - packages/dashboard/src/components/sidebar.tsx
  - .claude/hooks/gsd-brain-episode.js
  - GETTING-STARTED.md
  - CLAUDE.md
autonomous: true
requirements: [REN-06]

must_haves:
  truths:
    - "Dashboard PWA manifest shows 'Myco' as app name"
    - "Dashboard title bar shows 'Myco' not 'Brain Dashboard'"
    - "Dashboard sidebar shows 'Myco' not 'Brain'"
    - "GSD hook uses new myco directory path with BRAIN_DB_PATH fallback"
    - "GETTING-STARTED.md references myco names throughout"
  artifacts:
    - path: "packages/dashboard/vite.config.ts"
      provides: "PWA manifest with Myco branding"
      contains: "name: 'Myco'"
    - path: "packages/dashboard/index.html"
      provides: "HTML title with Myco"
      contains: "<title>Myco</title>"
    - path: "packages/dashboard/src/routes/index.tsx"
      provides: "Dashboard heading with Myco"
      contains: "Myco"
    - path: "packages/dashboard/src/components/sidebar.tsx"
      provides: "Sidebar branding with Myco"
      contains: "Myco"
  key_links:
    - from: "packages/dashboard/vite.config.ts"
      to: "PWA install prompt"
      via: "manifest.name"
      pattern: "name: 'Myco'"
    - from: ".claude/hooks/gsd-brain-episode.js"
      to: "~/.local/share/myco/brain.db"
      via: "getDbPath function"
      pattern: "myco.*brain\\.db"
---

<objective>
Update all user-facing branding (dashboard, PWA manifest, docs) and peripheral files (hooks, guides) from Brain/ai-workbots to Myco.

Purpose: Ensure every user-visible surface says "Myco" and documentation reflects the new names.
Output: Dashboard branding, PWA manifest, GETTING-STARTED.md, hooks, and CLAUDE.md all updated.
</objective>

<execution_context>
@/Users/olive/.claude/get-shit-done/workflows/execute-plan.md
@/Users/olive/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rename/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard branding and PWA manifest</name>
  <files>
    packages/dashboard/vite.config.ts,
    packages/dashboard/index.html,
    packages/dashboard/src/routes/index.tsx,
    packages/dashboard/src/components/sidebar.tsx
  </files>
  <read_first>
    packages/dashboard/vite.config.ts,
    packages/dashboard/index.html,
    packages/dashboard/src/routes/index.tsx,
    packages/dashboard/src/components/sidebar.tsx
  </read_first>
  <action>
    **packages/dashboard/vite.config.ts (PWA manifest):**
    - Line 31: Change `name: 'AI Workbots Brain'` to `name: 'Myco'`
    - Line 32: Change `short_name: 'Brain'` to `short_name: 'Myco'`
    - Line 33: Change `description: 'Agent knowledge graph dashboard'` to `description: 'Agent memory and knowledge graph'`

    **packages/dashboard/index.html:**
    - Line 8: Change `<title>Brain Dashboard</title>` to `<title>Myco</title>`

    **packages/dashboard/src/routes/index.tsx:**
    - Line 21: Change the h1 text from `Brain Dashboard` to `Myco Dashboard`

    **packages/dashboard/src/components/sidebar.tsx:**
    - Line 38: Change the sidebar title text from `Brain` to `Myco`
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/ai-workbots && grep "name: 'Myco'" packages/dashboard/vite.config.ts && grep "<title>Myco</title>" packages/dashboard/index.html && grep "Myco Dashboard" packages/dashboard/src/routes/index.tsx && grep "Myco" packages/dashboard/src/components/sidebar.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "name: 'Myco'" packages/dashboard/vite.config.ts returns 1
    - grep -c "short_name: 'Myco'" packages/dashboard/vite.config.ts returns 1
    - grep -c "AI Workbots" packages/dashboard/vite.config.ts returns 0
    - grep -c "Myco" packages/dashboard/index.html returns 1
    - grep -c "Brain Dashboard" packages/dashboard/index.html returns 0
    - grep -c "Myco Dashboard" packages/dashboard/src/routes/index.tsx returns 1
    - grep "Myco" packages/dashboard/src/components/sidebar.tsx succeeds
    - grep -c "Brain" packages/dashboard/src/components/sidebar.tsx returns 0
  </acceptance_criteria>
  <done>Dashboard title is "Myco", PWA manifest name and short_name are "Myco", sidebar says "Myco", heading says "Myco Dashboard".</done>
</task>

<task type="auto">
  <name>Task 2: Update hooks, GETTING-STARTED.md, and CLAUDE.md</name>
  <files>
    .claude/hooks/gsd-brain-episode.js,
    GETTING-STARTED.md,
    CLAUDE.md
  </files>
  <read_first>
    .claude/hooks/gsd-brain-episode.js,
    GETTING-STARTED.md,
    CLAUDE.md
  </read_first>
  <action>
    **.claude/hooks/gsd-brain-episode.js:**
    - Line 24: Add MYCO_DB_PATH check before BRAIN_DB_PATH:
      Current: `const envPath = process.env.BRAIN_DB_PATH;`
      New: `const envPath = process.env.MYCO_DB_PATH || process.env.BRAIN_DB_PATH;`
    - Line 28: Change `path.join(xdgData, 'ai-workbots', 'brain.db')` to `path.join(xdgData, 'myco', 'brain.db')`

    **GETTING-STARTED.md:**
    Read the full file and perform these replacements throughout:
    - Title: `# AI Workbots Brain -- Getting Started` -> `# Myco -- Getting Started`
    - All `brain-mcp` -> `myco` (the binary name)
    - All `brain-cli` -> `myco-cli`
    - All `ai-workbots` -> `myco` in paths and references (e.g., `~/.local/share/ai-workbots/brain.db` -> `~/.local/share/myco/brain.db`)
    - All `BRAIN_DB_PATH` -> `MYCO_DB_PATH` (but add a note that BRAIN_DB_PATH is still accepted as fallback)
    - The architecture diagram: update `brain-mcp  (MCP Server)` -> `myco  (MCP Server)`
    - The architecture diagram: update `~/.local/share/ai-workbots/brain.db` -> `~/.local/share/myco/brain.db`
    - The MCP config JSON: update the path if it references dist/index.js to use the new binary name context
    - Table entries: Database path, CLI tool name, env var name
    - Keep the meaning and structure identical; only change names

    **CLAUDE.md:**
    - Change `## Project` heading description: `**AI Workbots Brain**` -> `**Myco**`
    - Update the project description paragraph to say "Myco" instead of "AI Workbots Brain" where it appears as a name
    - Keep technical details (MCP server, SQLite, etc.) unchanged
    - Update any reference to `@ai-workbots/*` package names to `@myco/*`
    - Update any reference to `brain-mcp` / `brain-cli` to `myco` / `myco-cli`
    - Update any `BRAIN_DB_PATH` references to `MYCO_DB_PATH`
    - Update `~/.local/share/ai-workbots/` to `~/.local/share/myco/`
    - Do NOT modify the .planning/ references or GSD workflow section
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/ai-workbots && grep -c "ai-workbots" .claude/hooks/gsd-brain-episode.js && echo "FAIL: old ref in hook" || echo "PASS: hook clean"; grep -c "brain-mcp" GETTING-STARTED.md && echo "FAIL: old binary in docs" || echo "PASS: docs clean"; grep -c "AI Workbots Brain" CLAUDE.md && echo "FAIL: old name in CLAUDE.md" || echo "PASS: CLAUDE.md clean"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ai-workbots" .claude/hooks/gsd-brain-episode.js returns 0
    - grep -c "MYCO_DB_PATH" .claude/hooks/gsd-brain-episode.js returns 1
    - grep -c "BRAIN_DB_PATH" .claude/hooks/gsd-brain-episode.js returns 1 (fallback preserved)
    - grep -c "'myco'" .claude/hooks/gsd-brain-episode.js returns 1
    - grep -c "brain-mcp" GETTING-STARTED.md returns 0
    - grep -c "brain-cli" GETTING-STARTED.md returns 0
    - grep -c "myco-cli" GETTING-STARTED.md returns at least 1
    - grep -c "AI Workbots Brain" CLAUDE.md returns 0
    - grep "Myco" CLAUDE.md succeeds
    - grep -c "@ai-workbots" CLAUDE.md returns 0
  </acceptance_criteria>
  <done>GSD hook uses myco path with BRAIN_DB_PATH fallback, GETTING-STARTED.md uses all new names, CLAUDE.md says Myco throughout.</done>
</task>

</tasks>

<verification>
Run: `cd /Users/olive/Documents/GitHub/ai-workbots && grep -rn "AI Workbots Brain\|brain-mcp\|brain-cli" packages/dashboard/ GETTING-STARTED.md CLAUDE.md .claude/hooks/ | grep -v node_modules | grep -v dist | grep -v ".planning"`
Expected: Zero matches (except BRAIN_DB_PATH fallback references which are intentional).

Run: `grep "ai-workbots" .claude/hooks/gsd-brain-episode.js`
Expected: Zero matches.
</verification>

<success_criteria>
Dashboard PWA and UI show "Myco" everywhere, hook uses myco path with fallback, GETTING-STARTED.md and CLAUDE.md reference only new names.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rename/06-02-SUMMARY.md`
</output>

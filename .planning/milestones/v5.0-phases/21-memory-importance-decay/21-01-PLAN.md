---
phase: 21-memory-importance-decay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/decay.ts
  - packages/core/src/index.ts
  - packages/mcp-server/tests/decay.test.ts
autonomous: true
requirements: [DECAY-01, DECAY-02]

must_haves:
  truths:
    - "computeEffectiveConfidence returns base confidence unchanged for decay-exempt entities"
    - "computeEffectiveConfidence returns a decayed value lower than base confidence for non-exempt entities not accessed in 30+ days"
    - "Reinforcement count boosts effective confidence but never exceeds base confidence"
    - "Effective confidence never drops below 0.1 floor"
    - "The function is pure — takes only scalar inputs, no DB handle"
  artifacts:
    - path: "packages/core/src/decay.ts"
      provides: "computeEffectiveConfidence pure function and DECAY_EXEMPT_TYPES constant"
      exports: ["computeEffectiveConfidence", "DECAY_EXEMPT_TYPES"]
    - path: "packages/mcp-server/tests/decay.test.ts"
      provides: "Unit tests for decay function"
      min_lines: 40
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/decay.ts"
      via: "re-export"
      pattern: "export.*from.*decay"
---

<objective>
Create the pure `computeEffectiveConfidence` function and its unit tests.

Purpose: DECAY-01 requires observation importance to decay based on age and reinforcement. DECAY-02 requires this to be a pure read-time computation with no DB writes. This plan delivers the core algorithm as a tested, exported function before integration.
Output: `packages/core/src/decay.ts` with pure function, `packages/core/src/index.ts` updated with exports, comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-memory-importance-decay/21-CONTEXT.md
@.planning/phases/21-memory-importance-decay/21-RESEARCH.md

@packages/core/src/index.ts
@packages/core/src/types.ts

<interfaces>
<!-- Observation interface from packages/core/src/types.ts already has these fields: -->
<!-- last_accessed_at: string | null -->
<!-- decay_exempt: number (0 or 1 from SQLite INTEGER) -->
<!-- reinforcement_count: number -->
<!-- confidence: number -->

<!-- packages/core/src/index.ts current exports: -->
export { openDatabase } from './db.js';
export { runMigrations } from './migrations.js';
export { generateSessionId, buildProvenance } from './provenance.js';
export { loadConfig, getConfig } from './config.js';
export type { MycoConfig } from './config.js';
export { prepareStatements } from './statements.js';
export type { MycoStatements } from './statements.js';
export type { SourceType, Entity, Observation, Relationship, Episode, ApprovalQueueItem, ProvenanceRecord, ExtractedFact, ConsolidationSummary } from './types.js';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create decay.ts pure function with unit tests</name>
  <files>packages/core/src/decay.ts, packages/mcp-server/tests/decay.test.ts</files>
  <read_first>packages/core/src/types.ts, packages/core/src/index.ts, packages/mcp-server/tests/recall-filters.test.ts</read_first>
  <behavior>
    - Test: decay-exempt entity returns base confidence unchanged (confidence=0.9, decayExempt=true -> 0.9)
    - Test: recently accessed non-exempt entity (0 days ago) returns approximately base confidence (confidence=0.8, 0 days -> ~0.8)
    - Test: observation not accessed in 23 days decays to roughly half (confidence=1.0, lambda=0.03 -> ~0.5)
    - Test: observation not accessed in 30 days (default for null lastAccessedAt) decays significantly (confidence=1.0 -> ~0.407)
    - Test: reinforcement_count=5 boosts effective confidence by 0.5 but capped at base confidence
    - Test: floor of 0.1 is enforced even after extreme decay (confidence=0.2, 365 days -> 0.1)
    - Test: null lastAccessedAt treated as 30 days old
    - Test: injectable now parameter works correctly for deterministic testing
    - Test: DECAY_EXEMPT_TYPES contains exactly: preference, constraint, decision, architecture
  </behavior>
  <action>
    Create `packages/core/src/decay.ts` with:
    1. Export `DECAY_EXEMPT_TYPES` as `new Set(['preference', 'constraint', 'decision', 'architecture'])`.
    2. Export `computeEffectiveConfidence` as a pure function with this exact signature:
       ```
       computeEffectiveConfidence(params: {
         confidence: number;
         decayExempt: boolean;
         lastAccessedAt: string | null;
         reinforcementCount: number;
         now?: Date;
       }): number
       ```
    3. Constants: LAMBDA=0.03, REINFORCEMENT_WEIGHT=0.1, FLOOR=0.1, DEFAULT_DAYS_NOT_ACCESSED=30.
    4. Algorithm per locked decisions:
       - If decayExempt, return confidence unchanged.
       - Compute daysSinceAccess from lastAccessedAt (or DEFAULT_DAYS_NOT_ACCESSED if null).
       - `decayed = confidence * Math.exp(-LAMBDA * daysSinceAccess)`
       - `boost = reinforcementCount * REINFORCEMENT_WEIGHT`
       - `effective = Math.min(confidence, decayed + boost)` (boost cannot exceed base)
       - Return `Math.max(FLOOR, effective)`

    Create `packages/mcp-server/tests/decay.test.ts` with Vitest tests covering all behaviors listed above. Use injectable `now` parameter for deterministic assertions. Import from `@myco/core`.

    Follow existing test patterns from recall-filters.test.ts for import style and describe/it structure.
  </action>
  <verify>
    <automated>cd /Users/olive/gsd-workspaces/myco-v5/myco && npx vitest run packages/mcp-server/tests/decay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "computeEffectiveConfidence" packages/core/src/decay.ts returns the function export
    - grep "DECAY_EXEMPT_TYPES" packages/core/src/decay.ts returns the Set export
    - grep "LAMBDA.*0.03" packages/core/src/decay.ts confirms the decay constant
    - grep "FLOOR.*0.1" packages/core/src/decay.ts confirms the minimum floor
    - grep "Math.exp" packages/core/src/decay.ts confirms exponential decay formula
    - grep "decayExempt.*return confidence" packages/core/src/decay.ts confirms exempt bypass
    - grep "computeEffectiveConfidence" packages/mcp-server/tests/decay.test.ts returns test imports
    - grep "decay-exempt\|decay_exempt\|decayExempt" packages/mcp-server/tests/decay.test.ts confirms exempt test exists
  </acceptance_criteria>
  <done>computeEffectiveConfidence is a pure exported function. All unit tests pass. Decay-exempt returns base confidence. Non-exempt decays with exponential formula. Reinforcement boosts but caps at base. Floor of 0.1 enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Export decay module from @myco/core</name>
  <files>packages/core/src/index.ts</files>
  <read_first>packages/core/src/index.ts, packages/core/src/decay.ts</read_first>
  <action>
    Add these two export lines to `packages/core/src/index.ts`:
    ```
    export { computeEffectiveConfidence, DECAY_EXEMPT_TYPES } from './decay.js';
    ```
    Place after the existing `prepareStatements` export line. Use `.js` extension per existing convention in the file.
  </action>
  <verify>
    <automated>cd /Users/olive/gsd-workspaces/myco-v5/myco && npx vitest run packages/mcp-server/tests/decay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "computeEffectiveConfidence.*DECAY_EXEMPT_TYPES.*decay" packages/core/src/index.ts confirms the re-export exists
  </acceptance_criteria>
  <done>@myco/core exports computeEffectiveConfidence and DECAY_EXEMPT_TYPES. Downstream packages can import from @myco/core.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/mcp-server/tests/decay.test.ts` — all decay function tests pass
- `grep "export.*computeEffectiveConfidence" packages/core/src/decay.ts` — function is exported
- `grep "export.*DECAY_EXEMPT_TYPES" packages/core/src/decay.ts` — constant is exported
- `grep "computeEffectiveConfidence" packages/core/src/index.ts` — re-exported from @myco/core
</verification>

<success_criteria>
- computeEffectiveConfidence is a pure function that takes scalar inputs and returns a number
- No database imports or DB handle parameters in decay.ts
- All unit tests pass covering: exempt bypass, exponential decay, reinforcement boost, floor, null lastAccessedAt
- Function exported from @myco/core for use in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/21-memory-importance-decay/21-01-SUMMARY.md`
</output>

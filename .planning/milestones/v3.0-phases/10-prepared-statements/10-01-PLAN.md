---
phase: 10-prepared-statements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/statements.ts
  - packages/core/src/index.ts
  - packages/mcp-server/src/tools.ts
  - packages/mcp-server/src/consolidator.ts
  - packages/mcp-server/src/relationship-discovery.ts
  - packages/mcp-server/src/index.ts
  - packages/mcp-server/src/cli.ts
  - packages/core/tests/statements.test.ts
autonomous: true
requirements:
  - STMT-01
  - STMT-02

must_haves:
  truths:
    - "All MCP tool handlers use pre-compiled statements instead of inline db.prepare()"
    - "The statement factory is called once after applySchema(db) completes"
    - "No db.prepare() call exists inside any tool handler or request handler in mcp-server source"
    - "Existing tests still pass — no behavioral regression"
  artifacts:
    - path: "packages/core/src/statements.ts"
      provides: "Prepared statement factory"
      exports: ["prepareStatements", "MycoStatements"]
    - path: "packages/core/tests/statements.test.ts"
      provides: "Statement factory tests"
      min_lines: 30
  key_links:
    - from: "packages/mcp-server/src/index.ts"
      to: "packages/core/src/statements.ts"
      via: "prepareStatements(db) called after openDatabase()"
      pattern: "prepareStatements\\(db\\)"
    - from: "packages/mcp-server/src/tools.ts"
      to: "packages/core/src/statements.ts"
      via: "MycoStatements type used for all queries"
      pattern: "MycoStatements"
---

<objective>
Create a prepared statement factory in @myco/core and refactor all MCP server code (tools.ts, consolidator.ts, relationship-discovery.ts, cli.ts) to use pre-compiled statements instead of inline db.prepare() calls.

Purpose: Eliminate per-request statement compilation overhead on all hot-path queries in the MCP server. Prepared statements are compiled once at startup and reused for every tool invocation.

Output: `packages/core/src/statements.ts` with typed factory, all mcp-server source files refactored, test proving factory works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-prepared-statements/10-CONTEXT.md
@.planning/research/PITFALLS.md

@packages/core/src/db.ts
@packages/core/src/index.ts
@packages/core/src/schema.ts
@packages/mcp-server/src/tools.ts
@packages/mcp-server/src/consolidator.ts
@packages/mcp-server/src/relationship-discovery.ts
@packages/mcp-server/src/index.ts
@packages/mcp-server/src/cli.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create prepared statement factory with tests</name>
  <files>packages/core/src/statements.ts, packages/core/src/index.ts, packages/core/tests/statements.test.ts</files>
  <read_first>
    - packages/core/src/db.ts (openDatabase flow, applySchema call order)
    - packages/core/src/schema.ts (table definitions — know what columns exist)
    - packages/core/src/index.ts (current exports)
    - packages/mcp-server/src/tools.ts (all SQL strings to extract)
    - packages/mcp-server/src/consolidator.ts (all SQL strings to extract)
    - packages/mcp-server/src/relationship-discovery.ts (all SQL strings to extract)
    - packages/mcp-server/src/cli.ts (SQL strings — many overlap with tools.ts)
    - packages/api-server/src/routes/approvals.ts (SQL strings for Plan 02)
    - packages/api-server/src/routes/dashboard.ts (SQL strings for Plan 02)
    - packages/api-server/src/routes/entities.ts (SQL strings for Plan 02)
    - packages/api-server/src/routes/graph.ts (SQL strings for Plan 02)
    - packages/api-server/src/routes/episodes.ts (SQL strings for Plan 02)
  </read_first>
  <behavior>
    - Test 1: prepareStatements(db) returns an object with all expected statement keys (entity, observation, relationship, episode, approval, embedding, fts, consolidation groups)
    - Test 2: Each returned statement has .run and .get or .all methods (is a valid better-sqlite3 Statement)
    - Test 3: Statements work on a fresh database (openDatabase() with in-memory or temp path)
    - Test 4: Calling a statement (e.g. selectEntityByNameType.get('nonexistent', 'concept')) returns undefined (no crash)
    - Test 5: Insert + select round-trip works through prepared statements
  </behavior>
  <action>
    Create `packages/core/src/statements.ts` exporting:

    1. A `MycoStatements` interface with named statement properties, grouped by domain:
       - **Entity statements:**
         - `selectEntityByNameType`: `SELECT id FROM entities WHERE name = ? AND type = ?`
         - `insertEntity`: `INSERT INTO entities (id, name, type, summary, metadata, session_id, agent_id, source_type, confidence, created_at, updated_at) VALUES (?, ?, ?, NULL, '{}', ?, ?, ?, ?, ?, ?)`
         - `selectAllEntityNames`: `SELECT id, name FROM entities`
         - `deleteEntityById`: `DELETE FROM entities WHERE id = ?`
         - `updateEntityTimestampConfidence`: `UPDATE entities SET updated_at = ?, confidence = MAX(confidence, ?) WHERE id = ?`
         - `insertEntityFull`: `INSERT INTO entities (id, name, type, summary, metadata, session_id, agent_id, source_type, confidence, created_at, updated_at) VALUES (?, ?, ?, NULL, '{}', ?, ?, ?, ?, ?, ?)` (same as insertEntity — one statement serves both)
       - **Observation statements:**
         - `insertObservation`: `INSERT INTO observations (id, entity_id, content, metadata, session_id, agent_id, source_type, confidence, created_at) VALUES (?, ?, ?, '{}', ?, ?, ?, ?, ?)`
         - `insertObservationWithEmbeddingFlag`: `INSERT INTO observations (id, entity_id, content, metadata, session_id, agent_id, source_type, confidence, created_at, needs_embedding) VALUES (?, ?, ?, '{}', ?, ?, ?, ?, ?, 1)`
         - `flagObservationNeedsEmbedding`: `UPDATE observations SET needs_embedding = 1 WHERE id = ?`
         - `clearObservationEmbeddingFlag`: `UPDATE observations SET needs_embedding = 0 WHERE id = ?`
         - `selectPendingEmbeddings`: `SELECT o.id, o.content FROM observations o WHERE o.needs_embedding = 1 LIMIT ?`
         - `selectObservationsByEntityId`: `SELECT id, content, confidence, created_at FROM observations WHERE entity_id = ? ORDER BY created_at DESC LIMIT 20`
         - `updateObservationEntityId`: `UPDATE observations SET entity_id = ? WHERE entity_id = ?`
       - **Relationship statements:**
         - `insertRelationship`: `INSERT OR IGNORE INTO relationships (id, from_id, to_id, type, metadata, session_id, agent_id, source_type, confidence, created_at) VALUES (?, ?, ?, ?, '{}', ?, ?, ?, ?, ?)`
         - `selectRelationshipExists`: `SELECT 1 FROM relationships WHERE (from_id = ? AND to_id = ?) OR (from_id = ? AND to_id = ?)`
         - `updateRelationshipFromId`: `UPDATE relationships SET from_id = ? WHERE from_id = ?`
         - `updateRelationshipToId`: `UPDATE relationships SET to_id = ? WHERE to_id = ?`
       - **Embedding/vector statements:**
         - `insertVecEmbedding`: `INSERT INTO vec_embeddings (item_id, item_type, embedding) VALUES (?, ?, ?)`
         - `knnSearchObservations`: `WITH knn AS (SELECT item_id, distance FROM vec_embeddings WHERE embedding MATCH ? AND k = ? AND item_type = 'observation') SELECT o.id AS observation_id, o.content, o.confidence, e.name AS entity_name, e.type AS entity_type, knn.distance AS relevance_score FROM knn JOIN observations o ON o.id = knn.item_id JOIN entities e ON e.id = o.entity_id ORDER BY knn.distance`
         - `knnSearchForContradiction`: `WITH knn AS (SELECT item_id, distance FROM vec_embeddings WHERE embedding MATCH ? AND k = 5 AND item_type = 'observation') SELECT knn.distance FROM knn JOIN observations o ON o.id = knn.item_id WHERE o.entity_id = ? ORDER BY knn.distance LIMIT 1`
         - `knnSearchForRelationships`: `WITH knn AS (SELECT item_id, distance FROM vec_embeddings WHERE embedding MATCH ? AND k = 5 AND item_type = 'observation') SELECT DISTINCT o.entity_id, knn.distance FROM knn JOIN observations o ON o.id = knn.item_id WHERE o.entity_id != ? AND knn.distance < 0.25 ORDER BY knn.distance`
       - **FTS statements:**
         - `insertFtsObservation`: `INSERT INTO fts_observations (content, observation_id) VALUES (?, ?)`
         - `ftsSearchObservations`: `SELECT o.id AS observation_id, o.content, o.confidence, e.name AS entity_name, e.type AS entity_type, fts.rank AS relevance_score FROM fts_observations fts JOIN observations o ON o.id = fts.observation_id JOIN entities e ON e.id = o.entity_id WHERE fts_observations MATCH ? ORDER BY fts.rank LIMIT ?`
         - `ftsSearchEntityMentions`: `SELECT DISTINCT o.entity_id FROM fts_observations fts JOIN observations o ON o.id = fts.observation_id WHERE fts_observations MATCH ? LIMIT 100`
       - **Episode statements:**
         - `insertEpisode`: `INSERT INTO episodes (id, session_id, agent_id, event_type, payload, created_at) VALUES (?, ?, ?, ?, ?, ?)`
         - `selectUnconsolidatedEpisodes`: `SELECT id, session_id, agent_id, event_type, payload, created_at FROM episodes WHERE consolidated_at IS NULL ORDER BY created_at LIMIT ?`
       - **Approval queue statements:**
         - `selectPendingApprovals`: `SELECT id, item_type, item_id, status, reason, metadata, created_at FROM approval_queue WHERE status = 'pending' ORDER BY created_at LIMIT ?`
         - `selectApprovalById`: `SELECT id, item_type, metadata, status FROM approval_queue WHERE id = ?`
         - `updateApprovalStatus`: `UPDATE approval_queue SET status = ?, resolved_at = ? WHERE id = ?`
         - `insertApprovalQueueItem`: `INSERT INTO approval_queue (id, item_type, item_id, status, reason, metadata, created_at) VALUES (?, ?, ?, ?, ?, ?, ?)`

    2. A `prepareStatements(db: Database.Database): MycoStatements` factory function.

    CRITICAL CONSTRAINTS:
    - Do NOT reference a `project` column anywhere — it does not exist until Phase 12
    - The factory must accept any Database instance (tests use fresh in-memory DBs)
    - Use `better-sqlite3` Statement type for all properties
    - The `queryEntities` function in tools.ts builds dynamic WHERE clauses — it cannot be a single prepared statement. Leave it as inline db.prepare() with a code comment: `// Dynamic WHERE — cannot be pre-prepared (STMT-02 exception: query-time preparation with bound params)`
    - The `markBatchConsolidated` function in consolidator.ts uses dynamic IN(...) placeholders — leave it as inline db.prepare() with same exception comment

    3. Export `prepareStatements` and `MycoStatements` from `packages/core/src/index.ts`.

    Write tests in `packages/core/tests/statements.test.ts` following the behavior spec above.
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/myco && npx vitest run packages/core/tests/statements.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export function prepareStatements" packages/core/src/statements.ts returns 1
    - grep -c "export interface MycoStatements" packages/core/src/statements.ts returns 1
    - grep "prepareStatements" packages/core/src/index.ts shows the export
    - grep "MycoStatements" packages/core/src/index.ts shows the type export
    - No occurrence of the string "project" in packages/core/src/statements.ts (verified: grep -c "project" returns 0)
    - npx vitest run packages/core/tests/statements.test.ts exits 0
  </acceptance_criteria>
  <done>Statement factory exists in core with full type interface, all SQL strings extracted, tests pass, exported from package index. No reference to project column.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MCP server to use prepared statements</name>
  <files>packages/mcp-server/src/tools.ts, packages/mcp-server/src/consolidator.ts, packages/mcp-server/src/relationship-discovery.ts, packages/mcp-server/src/index.ts, packages/mcp-server/src/cli.ts</files>
  <read_first>
    - packages/core/src/statements.ts (the factory just created in Task 1)
    - packages/mcp-server/src/tools.ts (current inline prepare calls to replace)
    - packages/mcp-server/src/consolidator.ts (current inline prepare calls)
    - packages/mcp-server/src/relationship-discovery.ts (current inline prepare calls)
    - packages/mcp-server/src/index.ts (entry point — wire factory here)
    - packages/mcp-server/src/cli.ts (entry point — wire factory here)
    - packages/mcp-server/tests/server.test.ts (understand test setup to ensure compatibility)
  </read_first>
  <action>
    **index.ts changes:**
    After `const db = openDatabase();` add:
    ```
    import { prepareStatements } from '@myco/core';
    const stmts = prepareStatements(db);
    ```
    Update `registerTools(server, db)` to `registerTools(server, db, stmts)`.
    Pass `stmts` to `reEmbedPending(db)` is NOT needed because reEmbedPending already creates two local statements — refactor it to accept stmts instead.

    **tools.ts changes:**
    1. Change `registerTools` signature to `registerTools(server: McpServer, db: Database.Database, stmts: MycoStatements): void`
    2. Change `rememberEntity` signature to accept stmts as third parameter: `rememberEntity(db: Database.Database, params: RememberParams, stmts: MycoStatements)`
    3. Change `recallKnowledge` signature to accept stmts
    4. Change `logEpisode` signature to accept stmts
    5. Change `reEmbedPending` signature to accept stmts
    6. Replace every inline `db.prepare(...)` with the corresponding `stmts.xxx` property call
    7. For `queryEntities`: keep the dynamic WHERE clause as inline `db.prepare()` — add comment: `// Dynamic WHERE — cannot be pre-prepared (STMT-02 exception)`
       BUT extract the `getObservations` statement to use `stmts.selectObservationsByEntityId`
    8. For `list_pending_approvals` tool: use `stmts.selectPendingApprovals`
    9. For `resolve_approval` tool: use `stmts.selectApprovalById`, `stmts.updateApprovalStatus`, and merge-related statements
    10. For the relations loop in rememberEntity: use `stmts.selectEntityByNameType`, `stmts.insertEntity`, `stmts.insertRelationship`

    **consolidator.ts changes:**
    1. Change `runConsolidation` to accept stmts: `runConsolidation(db: Database.Database, stmts?: MycoStatements)`
       Make stmts optional for backward compat with tests — if not provided, fall back to inline prepare (but log a warning)
       Actually better: make it required and update callers. The consolidation tool handler in tools.ts passes stmts.
    2. Change `findMergeCandidates` to accept stmts and use `stmts.selectAllEntityNames`
    3. Change `detectContradiction` to accept stmts and use `stmts.knnSearchForContradiction`
    4. Replace `runConsolidation` inline prepare calls with stmts equivalents
    5. Keep `markBatchConsolidated` as inline db.prepare() — dynamic IN() clause, add exception comment
    6. The `rememberEntity` call inside consolidator already passes db — add stmts parameter

    **relationship-discovery.ts changes:**
    1. Change `discoverRelationships` to accept stmts as 5th parameter
    2. Change `createBackLinks` to accept stmts as 4th parameter
    3. Change `getEntityNames` to accept stmts and use `stmts.selectAllEntityNames`
    4. Replace all inline prepare calls with stmts equivalents:
       - relationship existence check: `stmts.selectRelationshipExists`
       - relationship insert: `stmts.insertRelationship`
       - KNN search: `stmts.knnSearchForRelationships`
       - FTS search: `stmts.ftsSearchEntityMentions`

    **cli.ts changes:**
    After `const db = openDatabase();` in each subcommand, add `const stmts = prepareStatements(db);`
    Replace all inline prepare calls with stmts equivalents.
    Update `runConsolidation(db)` calls to `runConsolidation(db, stmts)`.
    Update `rememberEntity(db, ...)` calls to `rememberEntity(db, ..., stmts)`.

    **Test compatibility:**
    The existing tests in `packages/mcp-server/tests/server.test.ts` call `rememberEntity(db, params)` directly.
    These tests must also pass stmts. Update the test helper setup to call `prepareStatements(db)` and pass stmts to function calls.
    Tests that use `db.prepare()` for assertions (e.g., verifying rows in DB) should keep their own inline prepare calls — those are test-only, not production hot paths.
  </action>
  <verify>
    <automated>cd /Users/olive/Documents/GitHub/myco && npx vitest run packages/mcp-server/tests/ && npx vitest run packages/core/tests/</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "db\.prepare(" packages/mcp-server/src/tools.ts returns ONLY lines inside queryEntities (the dynamic WHERE function) — no other occurrences
    - grep -rn "db\.prepare(" packages/mcp-server/src/consolidator.ts returns ONLY lines inside markBatchConsolidated — no other occurrences
    - grep -rn "db\.prepare(" packages/mcp-server/src/relationship-discovery.ts returns 0 lines
    - grep -rn "db\.prepare(" packages/mcp-server/src/index.ts returns 0 lines
    - grep -c "prepareStatements" packages/mcp-server/src/index.ts returns at least 1
    - grep -c "prepareStatements" packages/mcp-server/src/cli.ts returns at least 1
    - grep "STMT-02 exception" packages/mcp-server/src/tools.ts confirms the dynamic WHERE exception is documented
    - npx vitest run packages/mcp-server/tests/ exits 0
    - npx vitest run packages/core/tests/ exits 0
  </acceptance_criteria>
  <done>All MCP server source files use prepared statements from the factory. Only documented exceptions (dynamic WHERE in queryEntities, dynamic IN in markBatchConsolidated) retain inline prepare. All existing tests pass.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```bash
cd /Users/olive/Documents/GitHub/myco && npx vitest run
```

Verify no inline db.prepare() in hot paths:
```bash
# Should return ONLY queryEntities and markBatchConsolidated exceptions:
grep -rn "db\.prepare(" packages/mcp-server/src/ packages/core/src/ --include="*.ts" | grep -v "node_modules" | grep -v "\.test\." | grep -v "\.d\."
```

Verify statement factory is called at startup:
```bash
grep -n "prepareStatements" packages/mcp-server/src/index.ts packages/mcp-server/src/cli.ts
```
</verification>

<success_criteria>
1. `packages/core/src/statements.ts` exists with typed `prepareStatements(db)` factory
2. All MCP server tools (remember, recall, query, log_episode, consolidate, list_pending_approvals, resolve_approval) function correctly using prepared statements
3. No `db.prepare()` call exists inside any MCP tool handler function (except documented dynamic-query exceptions)
4. All existing tests pass without modification to test assertions (only test setup changes to pass stmts)
5. No reference to `project` column in any prepared statement
</success_criteria>

<output>
After completion, create `.planning/phases/10-prepared-statements/10-01-SUMMARY.md`
</output>
